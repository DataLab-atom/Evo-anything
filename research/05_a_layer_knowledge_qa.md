# A 层：知识检索与问答

> 统一的文献能力层——本地优先、外部补充、自动入库

## A1 在线文献检索

针对给定内容，从互联网检索相关文献并返回。

## A2 本地文献检索

从本地现存的文献库中检索匹配内容并返回答案。

## A3 代码问答

基于 baseline 代码、新代码及改动历史，回答传入的问题。

## A4 文献问答（对外接口）

- **输入**：一个需求/问题
- **流程**：
  1. 优先调用 **A2**（本地文献库）获取相关文献
  2. 数量不足时调用 **A1**（在线检索）补充
  3. 在线检索到的论文**自动写回本地文献库**
- **输出**：基于检索到的文献展开回答，附带 BibTeX 标准引用格式
